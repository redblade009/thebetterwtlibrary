baseURL: "https://github.io"
title: "Better WT Library"
theme: "blowfish"

hasCJKLanguage: true
enableRobotsTXT: true

outputs:
  home: ["HTML", "RSS", "JSON"] # Generates your complete paragraph full-text search index matrix

params:
  search:
    enable: true
    showDialogueHeader: true
    enableShortcuts: true
    layout: "canary"
