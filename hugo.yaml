baseURL: "https://github.io"
title: "Better WT Library"

module:
  imports:
    - path: "github.com/nunocoracao/blowfish/v2"

hasCJKLanguage: true
enableRobotsTXT: true

outputs:
  home: ["HTML", "RSS", "JSON"]

params:
  search:
    enable: true
    showDialogueHeader: true
    enableShortcuts: true
    layout: "canary"
